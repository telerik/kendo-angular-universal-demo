import { ElementRef, EventEmitter, OnDestroy, Renderer2 } from '@angular/core';
/**
 * Represents the Kendo UI TextBox directive for the Inputs components for Angular.
 * Used to style the textbox of any `input` element.
 *
 * @example
 * ```ts-no-run
 * <input kendoTextBox />
 * <input kendoTextBox type="email" />
 * <input kendoTextBox type="password" />
 * ```
 */
export declare class TextBoxDirective implements OnDestroy {
    private renderer;
    private inputElement;
    readonly hostClass: boolean;
    /**
     * Fires each time the user focuses the `input` element.
     */
    onFocus: EventEmitter<any>;
    /**
     * Fires each time the `input` element gets blurred.
     */
    onBlur: EventEmitter<any>;
    /**
     * Gets the value of the `input` element.
     */
    readonly value: string;
    id: string;
    private listeners;
    constructor(renderer: Renderer2, inputElement: ElementRef);
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
}
