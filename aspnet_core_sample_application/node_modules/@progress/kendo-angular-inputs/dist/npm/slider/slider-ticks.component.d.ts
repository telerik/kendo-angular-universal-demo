import { ElementRef, OnChanges, QueryList } from '@angular/core';
/**
 * @hidden
 */
export declare class SliderTicksComponent implements OnChanges {
    private rtl;
    wrapperClasses: string;
    tickTitle: (value: number) => string;
    vertical: boolean;
    step: number;
    min: number;
    max: number;
    tickElements: QueryList<ElementRef>;
    ticks: Array<any>;
    constructor(rtl: boolean);
    ngOnChanges(_: any): void;
    private createTicks();
    private endTickClasses(first);
}
