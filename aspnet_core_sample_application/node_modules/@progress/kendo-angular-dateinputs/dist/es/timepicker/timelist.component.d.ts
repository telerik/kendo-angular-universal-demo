import { EventEmitter, ElementRef, Renderer2, NgZone, Injector, OnChanges, OnInit, OnDestroy } from '@angular/core';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/auditTime';
import 'rxjs/add/operator/debounceTime';
import { DateFormatPart } from '@progress/kendo-angular-intl';
import { VirtualizationComponent } from '../virtualization/virtualization.component';
import { ListItem } from './models/list-item.interface';
import { ListService } from './models/list-service.interface';
import { DOMService } from './services/dom.service';
/**
 * @hidden
 */
export declare class TimeListComponent implements OnChanges, OnInit, OnDestroy {
    private element;
    private injector;
    private dom;
    private renderer;
    private zone;
    min: Date;
    max: Date;
    part: DateFormatPart;
    step: number;
    value: Date;
    valueChange: EventEmitter<Date>;
    virtualization: VirtualizationComponent;
    readonly tabIndex: number;
    readonly componentClass: boolean;
    animateToIndex: boolean;
    isActive: boolean;
    skip: number;
    total: number;
    service: ListService;
    itemHeight: number;
    listHeight: number;
    topOffset: number;
    bottomOffset: number;
    style: any;
    data: ListItem[];
    private indexToScroll;
    private scrollSubscription;
    private unsubscribeMouseOver;
    /**
     * @hidden
     */
    keydown(e: any): void;
    /**
     * @hidden
     */
    handleBlur(): void;
    /**
     * @hidden
     */
    handleFocus(): void;
    /**
     * @hidden
     */
    handleClick(): void;
    constructor(element: ElementRef, injector: Injector, dom: DOMService, renderer: Renderer2, zone: NgZone);
    ngOnChanges(changes: any): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngAfterViewInit(): void;
    ngAfterViewChecked(): void;
    handleChange(dataItem: ListItem): void;
    /**
     * Focuses the host element of the TimeList.
     *
     * @example
     * ```ts
     * _@Component({
     * selector: 'my-app',
     * template: `
     *  <button (click)="timelist.focus()">Focus TimeList</button>
     *  <kendo-timelist #timelist></kendo-timelist>
     * `
     * })
     * class AppComponent { }
     * ```
     */
    focus(): void;
    /**
     * Blurs the TimeList component.
     */
    blur(): void;
    private itemOffset(top);
    private hasMissingValue({previousValue, currentValue});
    private scrollOnce(action);
    private serviceSettings(settings?);
    private selectedIndex(value);
    private textHasChanged({previousValue, currentValue});
}
