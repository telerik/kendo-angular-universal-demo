import { ChangeDetectorRef, ElementRef, NgZone, OnDestroy, OnInit } from '@angular/core';
import 'rxjs/add/operator/delay';
import 'rxjs/add/operator/do';
import 'rxjs/add/operator/filter';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/switchMap';
import 'rxjs/add/operator/takeUntil';
import 'rxjs/add/operator/take';
import { ColumnBase } from '../columns/column-base';
import { DraggableDirective } from '../common/draggable.directive';
import { ColumnResizingService } from './column-resizing.service';
/**
 * @hidden
 */
export declare class ColumnHandleDirective implements OnInit, OnDestroy {
    draggable: DraggableDirective;
    private element;
    private service;
    private zone;
    private cdr;
    columns: Array<ColumnBase>;
    column: ColumnBase;
    readonly visible: string;
    private originalWidth;
    private subscriptions;
    autoFit(): void;
    constructor(draggable: DraggableDirective, element: ElementRef, service: ColumnResizingService, zone: NgZone, cdr: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private shouldUpdate();
    private initColumnWidth();
    private initState();
    private resize({deltaPercent});
    private sizeToFit({columns, widths});
    private updateWidth(column, width);
    private columnsForLevel(level);
}
