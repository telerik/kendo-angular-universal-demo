import { PopupService, PopupRef } from '@progress/kendo-angular-popup';
import { Renderer2, NgZone } from '@angular/core';
import { PreventableEvent } from '../../common/preventable-event';
import { Subject } from 'rxjs/Subject';
/**
 * @hidden
 */
export declare class SinglePopupService {
    private popupService;
    private renderer;
    private ngZone;
    private rtl;
    onClose: Subject<PreventableEvent>;
    private removeClick;
    private popupRef;
    constructor(popupService: PopupService, renderer: Renderer2, ngZone: NgZone, rtl?: boolean);
    open(anchor: any, template: any, popupRef: PopupRef): PopupRef;
    destroy(): void;
    private detachClose();
    private attachClose(skipElement);
}
