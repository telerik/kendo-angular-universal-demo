import { FilterService } from "../filter.service";
import { CompositeFilterDescriptor } from "@progress/kendo-data-query";
import { ColumnComponent } from '../../columns/column.component';
import { SinglePopupService } from './single-popup.service';
/**
 * @hidden
 */
export declare class FilterMenuComponent {
    protected filterService: FilterService;
    protected popupService: SinglePopupService;
    /**
     * The column with which the filter is associated.
     * @type {ColumnComponent}
     */
    column: ColumnComponent;
    /**
     * The current root filter.
     * @type {CompositeFilterDescriptor}
     */
    filter: CompositeFilterDescriptor;
    private popupRef;
    constructor(filterService: FilterService, popupService: SinglePopupService);
    readonly hasFilters: boolean;
    toggle(anchor: any, template: any): boolean;
    protected close(): void;
}
